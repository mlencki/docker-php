name: Push to GitHub Container Registry

on:
  push:
    branches: [master]

env:
  COMPOSER_VERSION: 2.0.8

jobs:
  push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: testing
        run: git diff v1.0.8 v1.0.9 --

